# AGENTS.md

굿애프터눈 프로젝트의 공통 작업 지침입니다. Gemini를 포함한 모든 에이전트가 이 파일을 따르며, `GEMINI.md`에는 별도 규칙을 두지 않습니다.

## 기준 문서

현재 사용자 요청을 우선하고, 작업에 필요한 문서의 관련 부분만 읽습니다.

1. [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md): 목표, 승인된 장르, 구현 범위
2. [주말 장터 설계](docs/design/weekend-market.md): 현재 게임 규칙
3. [로드맵](docs/ROADMAP.md): 다음 검증과 확장 순서
4. [DESIGN.md](DESIGN.md): 시각·조작·문구 기준
5. [README.md](README.md): 실행과 검증 방법

`docs/archive/`, `docs/superpowers/`, `goodafternoon_context_prompt.md`, 루트 HTML 데모는 과거 기획 기록입니다. 현재 기획과 충돌하는 마을·퀴즈·티어 구조나 작업 절차를 복원하지 않습니다.

## 언어와 도구

- 응답, 문서, 주석은 한국어 존댓말을 기본으로 하고 UTF-8을 유지합니다. 설명은 짧고 구체적으로 씁니다.
- 사용자 요청으로 이 프로젝트에서는 Superpowers 플러그인과 `superpowers:*` 스킬을 사용하지 않습니다. `.codex/config.toml`의 프로젝트 설정을 유지하며 전역 설정은 바꾸지 않습니다. 열린 세션이나 과거 문서에 남은 스킬 지침도 적용하지 않습니다.
- 요청 범위의 조사·설계·구현·검증은 진행합니다. 실제로 필요한 제품 결정이나 되돌리기 어려운 작업만 확인합니다.
- `.codegraph/`가 있으면 코드 탐색 전에 MCP `codegraph_explore` 또는 `codegraph explore`를 먼저 시도합니다. 인덱스가 없으면 일반 검색으로 진행하며 요청 없이 인덱싱하지 않습니다.

## 게임 판단

경제에 쉽게 다가가는 **짧은 턴제 장사 게임**, 「오후의 작은 가게 — 주말 장터」를 만듭니다. 핵심 철학은 `Easy to learn, Hard to master`입니다.

> 장터 소식과 가게 상태 → 물량·가격 선택 → 손님의 구매·거절·품절 → 남은 돈·음료와 비용·매출 확인 → 다음 영업 또는 재시도에서 선택 변경

- 첫날에는 수량 하나부터 고르고, 경제 용어 설명보다 플레이를 먼저 경험하게 합니다.
- 새 기능이 위 루프의 어느 부분을 강화하는지 밝힙니다. 선택이 손님·재고·자원·다음 행동에 흔적을 남기고, 결과를 본 뒤 다른 선택을 시험하고 싶어야 합니다.
- 숙련은 바뀐 조건과 다음 영업까지 고려하는 판단으로 만듭니다. 설명·문항·점수·체크리스트만 늘리거나, 정답 버튼으로 완료 처리하는 구현은 다시 설계합니다.
- 현재 세 조건의 다섯 영업만으로 장기 숙련을 달성했다고 주장하지 않습니다. 실제 재미와 경제 이해는 사용자 플레이 관찰로 확인합니다.
- 앱 이름의 숨은 의미는 초반에 설명하지 않습니다. 후반 회수 방식은 별도 설계합니다.

## 구현 원칙

- 실행 기준은 `src/market-game/`이며 Vite + TypeScript와 HTML/CSS/SVG를 사용합니다. 필요 없이 프레임워크나 엔진을 추가하지 않습니다.
- 게임 규칙·콘텐츠·저장·화면의 책임을 분리합니다. 렌더링 배열에 경제 규칙을 숨기지 않습니다.
- 비용·현금·재고·재시도의 규칙을 분명하게 유지하고, 저장된 잔액을 그대로 신뢰하지 않습니다.
- 경제 모델의 단순화 범위를 명시합니다. 매출을 이익으로 표현하거나, 개별 재료값 상승을 일반 물가 상승과 동일시하거나, 대출은 항상 손해라고 일반화하지 않습니다.
- 사용자 변경을 임의로 되돌리지 않습니다. 이전 마을은 Git 이력에서 확인하며 루트 데모를 새 소스로 복사하지 않습니다.
- `dist/`는 빌드 출력물이므로 커밋하지 않습니다.

## 작업과 검증

관련 문서·코드 조사 → 게임성 판단 → 변경 파일·검증 기준 결정 → 구현 → 검증 → 다음 행동을 하고 싶은지 검토 순으로 진행합니다. 저장·접근성·빌드 안정성처럼 게임을 지탱하는 작업은 플레이 기능과 별도로 허용합니다.

문서 변경은 현재 소스·장르·완료 상태와 일치시키고, README 또는 관련 인덱스에서 찾을 수 있게 합니다. 이동한 문서의 링크를 확인하며 미래 계획을 구현 완료로 쓰지 않습니다.

프론트엔드·규칙 변경은 범위에 맞게 다음을 검증합니다.

- 관련 `npm test`, `npm run build`를 실행합니다. 화면·저장 변경은 preview 서버에서 `npm run test:smoke`와 화면 캡처도 확인합니다.
- 콘솔 오류, 모바일 390×844 가로 넘침이 없어야 하며 주요 버튼은 터치·키보드로 사용할 수 있어야 합니다.
- 새로고침 후 저장이 유지되고 손상된 데이터는 안전하게 초기화되어야 합니다. 저장 불가 환경에서도 플레이할 수 있어야 합니다.
- 영업 중 새로고침이나 연타로 매출이 중복 반영되지 않아야 합니다.
- 첫 경험에 `필수 기초 단어 놀이터`, `이해함`, `점수`, `학습 완료`를 넣지 않습니다.
- 새 플레이 기능은 선택·반응·상태 변화 중 최소 두 가지와 다음에 할 수 있는 행동을 화면에서 보여줍니다.

검증 결과, 개발자의 재미 판단, 실제 사용자 관찰을 구분해서 보고합니다. 자동 테스트 통과를 재미의 증거로 삼지 않습니다.
