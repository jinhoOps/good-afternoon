# AGENTS.md

굿애프터눈 프로젝트의 공통 작업 지침입니다. 현재 사용자 요청과 `PROJECT_CONTEXT.md`를 기준으로 판단합니다.

## 프로젝트 도구 설정

- 사용자 요청으로 이 프로젝트에서는 Superpowers 플러그인과 `superpowers:*` 스킬을 사용하지 않습니다.
- `.codex/config.toml`의 프로젝트 설정으로 비활성화합니다. 전역 설정이나 다른 프로젝트의 플러그인은 변경하지 않습니다.
- 이미 열린 세션에 Superpowers 지침이 남아 있어도 적용하지 않습니다. 과거 `docs/superpowers/` 문서는 스킬 실행이나 단계별 승인 대기의 근거가 아닙니다.
- 사용자 요청 범위에서 조사, 설계, 구현, 검증을 진행합니다. 실제로 필요한 제품 결정이나 되돌리기 어려운 작업만 확인합니다.

## 작업 언어

- 응답, 문서, 주석은 한국어 존댓말을 기본으로 합니다.
- UTF-8을 유지하고 사용자 설명은 짧고 구체적으로 씁니다.

## 프로젝트 미션과 장르

경제에 쉽게 다가가는 게임을 만듭니다. 핵심 철학은 `Easy to learn, Hard to master`입니다.

현재 장르는 **짧은 턴제 장사 게임**이고, 실행 게임은 「오후의 작은 가게 — 주말 장터」입니다. 사용자가 기존 방식 전체의 재설계와 교체를 요청했습니다. 이전 마을·타일 이동·장소 네 곳 방문·퀴즈·티어는 새 게임의 필수 조건이 아닙니다.

- Easy to learn: 첫날에는 준비할 수량 하나를 고르고, 손님의 반응과 결과를 봅니다.
- Hard to master: 같은 조작으로 바뀐 조건을 읽고, 다음 영업까지 고려해 판단하게 만듭니다. 현재 고정된 다섯 날만으로 숙련 목표를 달성했다고 주장하지 않습니다.
- 경제 용어 설명보다 플레이를 먼저 둡니다. 실제 이해와 재미는 사용자 플레이 관찰로 확인합니다.
- 앱 이름의 숨은 의미는 초반에 설명하지 않습니다. 후반 회수 방식은 별도 설계합니다.

## 핵심 게임 루프

1. 장터 소식과 가게 상태를 봅니다.
2. 가진 자원으로 물량과 가격을 고릅니다.
3. 손님의 구매·가격 거절·품절 반응을 봅니다.
4. 남은 돈과 음료, 비용과 매출의 관계를 확인합니다.
5. 다음 날의 조건을 읽거나 같은 날을 다시 해보며 선택을 바꿉니다.

새 플레이 기능은 이 루프의 어느 부분을 강화하는지 밝힙니다. 저장, 접근성, 빌드 안정성 등 게임을 지탱하는 작업은 별도로 허용합니다.

## 재미 우선 원칙

- 선택: 상황을 읽고 자신의 예측을 시험할 수 있어야 합니다.
- 결과: 선택이 손님, 재고, 자원, 이후 가능한 행동에 흔적을 남겨야 합니다.
- 호기심: 결과를 보고 다음 선택을 바꿔보고 싶어야 합니다.
- 숙련: 상황이 바뀌면 같은 행동의 가치도 달라져야 합니다.

설명문·체크리스트만 늘리거나, 정답 버튼으로 완료 처리하거나, 점수와 문항 수를 핵심 보상으로 내세우는 구현은 다시 설계합니다. 게임으로 보이는 화면만 붙이고 실제 판단은 없는 경우도 같습니다.

## 문서 우선순위

1. `PROJECT_CONTEXT.md`: 현재 목표, 장르, 범위
2. `docs/design/weekend-market.md`: 구현된 게임 규칙과 재미 검토
3. `docs/ROADMAP.md`: 다음 검증과 확장 후보
4. `DESIGN.md`: 시각·조작·문구 기준
5. `README.md`: 실행과 검증 안내

`harness/`, `docs/superpowers/`, 과거 기획과 루트 HTML 데모는 참고 기록입니다. 현재 기획과 충돌하는 예전 확정 구조를 복원하지 않습니다. `GEMINI.md`는 Gemini용 harness 인덱스입니다.

## 개발 원칙

- 현재 구현 기준은 `src/market-game/`입니다. 과거 마을은 Git 이력에서 확인하며 새 소스의 기준이 아닙니다.
- Vite + TypeScript와 HTML/CSS/SVG를 사용합니다. 필요 없이 프레임워크나 엔진을 추가하지 않습니다.
- `dist/`는 빌드 출력물이며 커밋하지 않습니다.
- 게임 규칙, 콘텐츠, 저장, 화면의 책임을 분리합니다. 렌더링 배열에 경제 규칙을 숨기지 않습니다.
- 비용, 현금 보존, 재고, 재시도의 규칙을 분명하게 유지합니다. 저장된 잔액을 그대로 신뢰하지 않습니다.
- 사용자 변경을 임의로 되돌리지 않습니다. 루트 데모를 새 소스로 복사하지 않습니다.

## CodeGraph

`.codegraph/`가 있는 저장소에서 코드를 이해하거나 찾을 때에는 CodeGraph를 먼저 시도합니다. MCP `codegraph_explore` 또는 `codegraph explore`를 사용합니다. 인덱스가 없다면 사용자가 요청하지 않은 인덱싱은 하지 않고 일반 검색으로 진행합니다.

## 작업 루프

1. Research: 관련 문서와 코드를 읽습니다.
2. Game Check: 강화할 선택·결과·호기심·숙련과 경제 기초를 정합니다.
3. Strategy: 바꿀 파일과 검증 기준을 정합니다.
4. Execution: 책임이 분명한 변경을 적용합니다.
5. Validation: 변경 범위에 맞는 규칙·브라우저·모바일 검증을 합니다.
6. Fun Review: 다음 행동을 하고 싶어지는지 평가하고, 사용자 관찰과 개발자 판단을 구분합니다.

## 검증 기준

문서 변경은 현재 소스·장르·완료 상태와 맞아야 하며 README 또는 관련 인덱스에서 발견 가능해야 합니다. 미래 계획을 구현 완료로 쓰지 않습니다.

프론트엔드와 규칙 변경:

- 관련 `npm test`, `npm run build`, 브라우저 스모크를 수행합니다.
- 콘솔 오류와 모바일 390×844 가로 넘침이 없어야 합니다.
- 저장은 새로고침 후 유지하고 손상된 데이터는 안전하게 초기화합니다. 저장 불가 환경에서도 플레이할 수 있어야 합니다.
- 영업 중 새로고침이나 연타로 매출이 중복 반영되지 않아야 합니다.
- 주요 버튼은 터치와 키보드로 사용할 수 있어야 합니다.
- 첫 경험에 `필수 기초 단어 놀이터`, `이해함`, `점수`, `학습 완료`를 넣지 않습니다.
- 새 플레이 기능은 선택·반응·상태 변화 중 최소 두 가지를 화면에서 확인할 수 있어야 합니다.
- 다음에 할 수 있는 행동을 화면에 보여줍니다.

경제 모델은 단순화한 범위를 명시합니다. 개별 재료값 상승을 일반 물가 상승과 동일시하거나, 매출을 이익으로 표현하거나, 대출은 항상 손해라는 식의 잘못된 일반화를 만들지 않습니다.
